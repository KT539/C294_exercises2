# C294_exercises2

KT539 = Kilian Testard


npm install -g json-server pour installer le json-server local.

npm run backend dans un deuxième terminal pour lancer le json-server local.

npm run dev pour la liste d'events avec CRUD.